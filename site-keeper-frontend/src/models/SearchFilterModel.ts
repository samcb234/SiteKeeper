class SearchFilter {
    phrase: string
    filter: string

    constructor(phrase: string, filter: string){
        this.phrase = phrase
        this.filter = filter
    }
}

export default SearchFilter